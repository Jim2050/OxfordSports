# Nixpacks Configuration for Railway
# This tells Railway how to build your Node.js backend

[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm ci --omit=dev']

[start]
cmd = 'node server.js'
